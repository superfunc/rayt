.global _main

_main
        mov     r1, #200        ; size of x dimension
        mov     r2, #100        ; size of y dimension
        mov     r3, #0          ; y position counter
        mov     r4, #0          ; x position counter
        
loop
        mov         
        sub     r0, r0, #1
        cmp     r0, #0
        bne     loop

